# ============================================================
# FILE MẪU - copy file này thành ".env" rồi điền giá trị THẬT
# KHÔNG commit file .env (chứa mật khẩu) lên GitHub
# ============================================================

# --- Mật khẩu cho MySQL (bạn tự đặt, đặt mạnh một chút) ---
DB_PASSWORD=DatMatKhauManhVaoDay

# --- Chuỗi bí mật ký token đăng nhập (chuỗi base64 dài, ngẫu nhiên) ---
JWT_BASE64_SECRET=

# --- Email gửi thư (Gmail) + App Password 16 ký tự ---
MAIL_USERNAME=email_cua_ban@gmail.com
MAIL_PASSWORD=

# --- Key AI ---
GROQ_API_KEY=
PINECONE_API_KEY=

# --- Đăng nhập Google (để trống nếu không dùng) ---
GOOGLE_CLIENT_ID=

# --- Tài khoản admin đầu tiên (dùng để đăng nhập trang quản trị) ---
ADMIN_EMAIL=admin@jobfind.com
ADMIN_PASSWORD=DatMatKhauAdminVaoDay
